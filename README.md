# ПМ АТВ - Программный модуль автоматизированного тестирования веб-форм

Дипломный проект по разработке программного модуля для автоматизированного тестирования веб-форм с использованием временных email адресов и генерации тестовых данных.

## 📋 Описание проекта

ПМ АТВ - это комплексное решение для автоматизированного тестирования веб-форм, состоящее из:
- **Бэкенд** (Python + FastAPI) - API сервер, SMTP сервер, WebSocket для real-time уведомлений
- **Браузерное расширение** (Chrome Extension) - генератор данных, автоматическое заполнение форм, мониторинг почты

## 🚀 Быстрый старт

### Требования

- Python 3.10+
- PostgreSQL 14+
- Node.js (опционально, для разработки расширения)
- Chrome/Edge браузер

### Установка и запуск бэкенда

1. **Клонируйте репозиторий и перейдите в папку бэкенда:**
```bash
cd backend
```

2. **Создайте виртуальное окружение:**
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте базу данных:**
```bash
# Создайте БД PostgreSQL
createdb atv_db

# Или используйте Docker:
cd ../docker
docker-compose up -d
```

5. **Настройте переменные окружения:**
Создайте файл `.env` в папке `backend/`:
```env
DATABASE_URL=postgresql://atv_user:atv_password@localhost:5432/atv_db
SMTP_HOST=0.0.0.0
SMTP_PORT=1025
API_HOST=0.0.0.0
API_PORT=8000
EMAIL_TTL_HOURS=24
```

6. **Запустите сервер:**
```bash
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Сервер будет доступен по адресу: http://localhost:8000
API документация: http://localhost:8000/docs

### Установка браузерного расширения

1. **Откройте Chrome/Edge и перейдите в режим разработчика:**
   - Chrome: `chrome://extensions/`
   - Edge: `edge://extensions/`

2. **Включите "Режим разработчика"**

3. **Нажмите "Загрузить распакованное расширение"**

4. **Выберите папку `extension/` из проекта**

5. **Расширение готово к использованию!**

## 📖 Использование

### Работа с расширением

1. **Генерация тестовых данных:**
   - Откройте popup расширения
   - Нажмите "Сгенерировать данные"
   - Данные автоматически создаются на русском языке

2. **Создание временного email:**
   - Нажмите "Создать email"
   - Будет создан уникальный временный адрес
   - WebSocket подключится автоматически для получения писем в реальном времени

3. **Заполнение формы:**
   - Откройте страницу с формой
   - Нажмите "Заполнить форму на странице" в popup
   - Форма будет автоматически заполнена сгенерированными данными

4. **Мониторинг писем:**
   - Все входящие письма отображаются в popup
   - Уведомления приходят в реальном времени через WebSocket

### API Endpoints

- `POST /api/v1/email` - Создать временный email
- `GET /api/v1/email/{email_id}` - Получить информацию о ящике
- `GET /api/v1/email/{email_id}/messages` - Получить все письма
- `GET /api/v1/messages/{message_id}` - Получить конкретное письмо
- `DELETE /api/v1/email/{email_id}` - Удалить ящик
- `WS /api/v1/ws/{email_id}` - WebSocket для real-time уведомлений

## 🏗️ Архитектура

### Бэкенд
- **FastAPI** - REST API и WebSocket сервер
- **SQLAlchemy** - ORM для работы с БД
- **PostgreSQL** - основная база данных
- **aiosmtpd** - асинхронный SMTP сервер
- **Pydantic** - валидация данных

### Фронтенд (расширение)
- **Manifest V3** - современный формат расширений Chrome
- **Content Scripts** - автоматическое заполнение форм
- **Background Service Worker** - обработка фоновых задач
- **WebSocket Client** - подключение к серверу для real-time обновлений

## 📁 Структура проекта

```
Diploma-ATV/
├── backend/                 # Бэкенд приложение
│   ├── app/
│   │   ├── api/            # API endpoints и WebSocket
│   │   ├── core/           # Конфигурация
│   │   ├── models.py       # Модели БД
│   │   ├── schemas.py      # Pydantic схемы
│   │   ├── services/       # Бизнес-логика
│   │   └── main.py         # Точка входа
│   ├── requirements.txt
│   └── .env
├── extension/              # Браузерное расширение
│   ├── manifest.json
│   ├── popup.html/js      # UI расширения
│   ├── content.js         # Скрипт для заполнения форм
│   ├── background.js      # Background worker
│   ├── data-generator.js  # Генератор тестовых данных
│   └── icons/             # Иконки расширения
├── docker/                # Docker конфигурация
└── README.md
```

## 🔧 Тестирование SMTP

Для тестирования приема писем можно использовать команду:

```bash
# Отправить тестовое письмо на временный email
echo "Тестовое письмо" | mail -s "Тест" user123@temp.atv.local -S smtp=localhost:1025
```

Или используйте Python:
```python
import smtplib
from email.message import EmailMessage

msg = EmailMessage()
msg['From'] = 'test@example.com'
msg['To'] = 'your-temp-email@temp.atv.local'
msg['Subject'] = 'Тестовое письмо'
msg.set_content('Тело письма')

smtp = smtplib.SMTP('localhost', 1025)
smtp.send_message(msg)
smtp.quit()
```

## ⚠️ Примечания

- SMTP сервер запускается автоматически при старте FastAPI приложения
- Временные email адреса автоматически удаляются через 24 часа (настраивается)
- Для production использования рекомендуется настроить HTTPS/WSS
- CORS настроен для всех источников (для разработки)

## 📝 Лицензия

Дипломный проект. Использование только в образовательных целях.

## 👤 Автор

Джугели Д.А. ПИН-51Д