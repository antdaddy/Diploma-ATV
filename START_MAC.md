# 🚀 Инструкция по запуску на macOS

## Шаг 1: Установка Docker Desktop

1. Скачайте Docker Desktop для Mac: https://www.docker.com/products/docker-desktop
2. Установите и запустите Docker Desktop
3. Дождитесь, пока Docker запустится (иконка в строке меню должна быть зеленая)

## Шаг 2: Запуск базы данных PostgreSQL

```bash
# Перейдите в папку с docker-compose
cd /Users/ant_daddy/Desktop/Diploma-ATV_proj/Diploma-ATV/docker

# Запустите PostgreSQL в Docker
docker-compose up -d

# Проверьте, что контейнер запущен
docker-compose ps
```

Должно показать что-то вроде:
```
NAME                STATUS          PORTS
docker_postgres_1   Up (healthy)    0.0.0.0:5432->5432/tcp
```

## Шаг 3: Настройка Python окружения

```bash
# Перейдите в папку бэкенда
cd /Users/ant_daddy/Desktop/Diploma-ATV_proj/Diploma-ATV/backend

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте его
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

## Шаг 4: Запуск бэкенд сервера

```bash
# Убедитесь, что виртуальное окружение активно (должно быть (venv) в начале строки)
# Если нет - выполните: source venv/bin/activate

# Запустите сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Вы должны увидеть:
```
INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     SMTP сервер запущен на 0.0.0.0:1025
✅ Таблицы готовы
```

**Оставьте этот терминал открытым!** Сервер должен работать.

## Шаг 5: Проверка работы API

Откройте новый терминал и проверьте:

```bash
# Проверка здоровья сервера
curl http://localhost:8000/health

# Должно вернуть: {"status":"healthy"}

# Создание тестового email
curl -X POST http://localhost:8000/api/v1/email

# Должно вернуть JSON с email адресом и ID
```

Или откройте в браузере:
- http://localhost:8000/docs - интерактивная документация API
- http://localhost:8000/health - проверка здоровья

## Шаг 6: Установка браузерного расширения

1. Откройте **Chrome** или **Edge**
2. Перейдите на `chrome://extensions/` (или `edge://extensions/`)
3. В правом верхнем углу включите **"Режим разработчика"** (Developer mode)
4. Нажмите **"Загрузить распакованное расширение"** (Load unpacked)
5. Выберите папку: `/Users/ant_daddy/Desktop/Diploma-ATV_proj/Diploma-ATV/extension`
6. Расширение должно появиться в списке

## Шаг 7: Тестирование всего функционала

### Тест 1: Генерация данных
1. Откройте любую страницу в браузере
2. Нажмите на иконку расширения (в панели инструментов)
3. Нажмите **"Сгенерировать данные"**
4. Должны появиться тестовые данные (ФИО, телефон, адрес и т.д.)

### Тест 2: Создание временного email
1. В popup расширения нажмите **"Создать email"**
2. Должен появиться email адрес (например: `abc123@temp.atv.local`)
3. Статус должен измениться на **"Подключено"** (зеленый)

### Тест 3: Отправка тестового письма
В новом терминале:

```bash
# Сначала получите email адрес через API
EMAIL=$(curl -s -X POST http://localhost:8000/api/v1/email | python3 -c "import sys, json; print(json.load(sys.stdin)['email'])")

# Отправьте тестовое письмо
cd /Users/ant_daddy/Desktop/Diploma-ATV_proj/Diploma-ATV/backend
python3 test_email.py $EMAIL
```

Или вручную:
```bash
cd /Users/ant_daddy/Desktop/Diploma-ATV_proj/Diploma-ATV/backend
python3 test_email.py ваш-email@temp.atv.local
```

### Тест 4: Проверка получения письма
1. После отправки письма откройте popup расширения
2. В разделе **"Входящие письма"** должно появиться письмо
3. Или нажмите **"Обновить"** для обновления списка

### Тест 5: Заполнение формы
1. Откройте любую страницу с формой (например, https://example.com или любой сайт с формой)
2. Откройте popup расширения
3. Сгенерируйте данные (если еще не сделали)
4. Нажмите **"Заполнить форму на странице"**
5. Форма должна автоматически заполниться данными

## 🐛 Устранение проблем

### Docker не запускается
```bash
# Проверьте статус Docker
docker ps

# Если ошибка - перезапустите Docker Desktop через приложение
```

### Ошибка подключения к БД
```bash
# Проверьте, что PostgreSQL контейнер запущен
docker-compose ps

# Проверьте логи
docker-compose logs postgres

# Перезапустите контейнер
docker-compose restart
```

### Порт 8000 занят
```bash
# Найдите процесс, использующий порт
lsof -i :8000

# Убейте процесс или измените порт в .env файле
```

### Расширение не работает
1. Откройте консоль разработчика в popup (правый клик на popup → Inspect)
2. Проверьте ошибки в консоли
3. Убедитесь, что бэкенд запущен на localhost:8000

### WebSocket не подключается
- Проверьте, что сервер запущен
- Откройте консоль браузера (F12) и проверьте ошибки
- Убедитесь, что используете `http://localhost:8000`, а не `https://`

## 📝 Полезные команды

```bash
# Остановить Docker контейнеры
cd docker
docker-compose down

# Посмотреть логи PostgreSQL
docker-compose logs -f postgres

# Пересоздать БД с нуля
docker-compose down -v
docker-compose up -d

# Проверить все запущенные контейнеры
docker ps

# Остановить бэкенд сервер
# Нажмите Ctrl+C в терминале где запущен uvicorn
```

## ✅ Чеклист готовности

- [ ] Docker Desktop установлен и запущен
- [ ] PostgreSQL контейнер запущен (`docker-compose ps`)
- [ ] Python виртуальное окружение создано и активировано
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Бэкенд сервер запущен на http://localhost:8000
- [ ] API доступен (проверено через /health)
- [ ] Браузерное расширение установлено
- [ ] Генерация данных работает
- [ ] Создание email работает
- [ ] WebSocket подключение работает (статус "Подключено")
- [ ] Отправка и получение писем работает
- [ ] Заполнение форм работает